{
  "name": "@ast24/hmbt-v5-admin-alert",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:lib": "npm --prefix ../lib install && npm --prefix ../lib run build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "dev": "npm run build:lib && wrangler dev",
    "deploy": "npm run build:lib && wrangler deploy"
  },
  "dependencies": {
    "@ast24/hmbt-v5-lib": "file:../lib"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
